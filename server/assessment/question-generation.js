const { call: callOpenRouter, stream: streamOpenRouter } = require("../ai/gateway");
const { parseJson } = require("../ai/response-parser");
const alignment = require("../harness/alignment");

const SINGLE_SUBSTANCE_RULES = ["Setiap soal hanya menanyakan SATU substansi (satu kompetensi/tujuan pembelajaran).", "DILARANG pertanyaan bertingkat yang meminta beberapa hal sekaligus dalam satu soal.", "Setiap soal cukup SATU kalimat tanya dengan SATU tanda tanya; hindari kata penghubung tugas ganda.", "Jika konteks diperlukan, tulis konteks sebagai kalimat informasi di awal soal, lalu akhiri SATU pertanyaan/instruksi tunggal yang bisa dijawab lisan."].join(" ");
const ORAL_SCENARIO_RULES = ["Soal harus dirancang untuk skenario UJIAN LISAN: dijawab langsung, spontan, realtime lewat lisan.", "Soal harus mengukur CARA dan PROSES berpikir siswa.", "DILARANG soal tertutup yang cukup dijawab ya/tidak, satu angka, satu nama, atau satu kata.", "HINDARI soal hafalan semata bila tidak menuntut siswa menafsirkan, menerapkan, atau menjelaskan alasan.", "Tetap patuhi prinsip SATU substansi dan satu tujuan."].join(" ");

function buildMessages(payload) {
  const count = Number(payload.count || 5);
  const criteria = alignment.parseRubricCriteria(payload) || [];
  return [{ role: "user", content: JSON.stringify({ tugas: "Buat soal assessment lisan satu per satu sesuai konfigurasi guru.", topik: payload.topic, learning_outcome: payload.outcomes, rubrik: payload.rubric, kriteria_rubrik_yang_tersedia: criteria.map((c) => ({ id: c.id, nama: c.name, bobot: c.weight })), aturan_penyelarasan_soal_dengan_rubrik: ["Tulis setiap soal agar isi yang ditanyakan persis dapat dinilai oleh SUBSET kriteria rubrik.", "Isi criteria hanya dengan kriteria yang BENAR-BENAR diuji oleh soal tersebut.", "Soal tidak boleh menanyakan hal yang tidak diukur oleh kriteria mana pun.", "Seluruh kriteria yang relevan wajib tercakup setidaknya satu soal."].join(". "), aturan_rubrik_per_soal: "Buat rubric khusus untuk setiap soal berdasarkan pertanyaan dan learning_outcome. Setiap criterion harus memiliki evidence demand yang eksplisit di pertanyaan. Jangan menambahkan indikator contoh, penerapan, alasan, analisis, perbandingan, atau evaluasi jika pertanyaan tidak memintanya.", tingkat_kesulitan: payload.difficulty, contoh_soal_opsional: payload.examples || "", aturan_penulisan_soal: SINGLE_SUBSTANCE_RULES, aturan_skenario_ujian_lisan: ORAL_SCENARIO_RULES, jumlah_soal: count }) }];
}
const SCHEMA = 'Format: {"questions":[{"prompt":"...","focus":"...","outcome":"...","rubric":"indikator 40%\\nindikator 35%\\nindikator 25%","ideal":"...","criteria":["nama_kriteria1","nama_kriteria2"]}]}. rubric WAJIB khusus untuk setiap soal. Jumlah questions harus sesuai jumlah_soal.';
function buildFallbackRubric(question, payload) { const focus = String(question.focus || payload.topic || "konsep").trim(); const outcome = String(question.outcome || payload.outcomes || "kompetensi pembelajaran").trim(); return [`Ketepatan menjawab pertanyaan tentang ${focus}: 40%`, `Keselarasan dengan learning outcome (${outcome}): 30%`, "Kelengkapan evidence yang diminta pertanyaan: 20%", "Kejelasan penyampaian jawaban: 10%"].join("\n"); }
function normalizeQuestion(payload) { return (question, index) => ({ id: `q-ai-${Date.now()}-${index}`, prompt: String(question.prompt || "").trim(), focus: String(question.focus || payload.topic || "konsep").trim(), outcome: String(question.outcome || payload.outcomes || "").trim(), rubric: String(question.rubric || "").trim() || buildFallbackRubric(question, payload), ideal: String(question.ideal || "Jawaban kuat sesuai rubrik guru.").trim(), criteria: Array.isArray(question.criteria) ? question.criteria.map(String) : [] }); }
async function generateQuestions(payload) { const count = Number(payload.count || 5); const result = await callOpenRouter(buildMessages(payload), SCHEMA, { tenantId: payload.tenantId, userId: payload.userId, action: "generate-questions" }); if (!Array.isArray(result.questions)) throw new Error("Model tidak mengembalikan daftar soal"); return result.questions.slice(0, count).map(normalizeQuestion(payload)); }
async function streamGenerateQuestions(payload, onChunk) { const count = Number(payload.count || 5); const { content } = await streamOpenRouter(buildMessages(payload), SCHEMA, { tenantId: payload.tenantId, userId: payload.userId, action: "generate-questions" }, onChunk); const parsed = parseJson(content); if (!Array.isArray(parsed.questions)) throw new Error("Model tidak mengembalikan daftar soal"); return parsed.questions.slice(0, count).map(normalizeQuestion(payload)); }
module.exports = { generateQuestions, streamGenerateQuestions, buildMessages, normalizeQuestion, SINGLE_SUBSTANCE_RULES, ORAL_SCENARIO_RULES };